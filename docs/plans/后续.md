# 后续计划

> 已完成的演进记录见 [changes/](../../changes/) 和 [2026-04-12-agent-system-overhaul.md](./2026-04-12-agent-system-overhaul.md)

## 已完成

| 功能 | 说明 | 完成日期 |
|------|------|---------|
| AI 定时任务系统 | 自然语言创建/管理定时任务，独立执行 Agent，分层重试 | 2026-04-14 |
| Supervisor 路由优化 | HIDDEN_AGENTS 机制，工具列表辅助路由 | 2026-04-14 |
| 内置 Agent 同步增强 | 启动时自动同步 tools/system_prompt/capabilities | 2026-04-14 |
| 定时任务结果通知 | 全局铃铛通知 + 会话内 system 消息双重通知 | 2026-04-14 |
| 定时任务 allowed_tools 白名单 | 限制执行 Agent 可调用的工具范围 | 2026-04-14 |
| 会话页面交互优化 | 滚动到底部按钮 + 条件自动滚动 + 移动端侧边栏抽屉 + 会话搜索 + Textarea 自动增高 + 空状态卡片可操作 + 会话标题延迟刷新 + Alt+Space 快捷键录音 + Playwright E2E 测试（42 项） | 2026-04-22 |
| Multi-Agent 短期改进 | Agent 私有记忆隔离（agent_id scope）+ Handoff 上限保护（max_turns）+ Fast-path 快速路由（关键词意图匹配跳过 Supervisor） | 2026-04-22 |
| Avatar 系统 Phase 0 | Live2D 角色基础框架：PixiJS 6 + Cubism 4 集成，独立体验页 `/avatar`，表情/动作/口型同步接口 | 2026-04-22 |

## 待实现

| 优先级 | 功能 | 说明 |
|--------|------|------|
| 高 | HITL（Human-in-the-Loop）确认机制 | `execute_command`、`write_file` 等高风险工具执行前需用户确认 |
| 高 | 旧 `graph.builder.ts` 清理 | `llm/` 目录已删除，但 `langgraph.service.ts` 仍 import `buildGraph`，需迁移到 Supervisor 后移除 |
| 中 | Avatar 情绪驱动（Phase 1） | SSE 流加 emotion 事件，LLM 输出情绪标签自动驱动 Live2D 表情 |
| 中 | Avatar TTS 口型同步（Phase 2） | TTS 播放时 AnalyserNode 提取音量驱动角色嘴巴开合 |
| 中 | Token 计算 + 消息截断 | 无滑动窗口，依赖模型自身截断，长对话会丢失上下文 |
| 中 | per-Agent 上下文管理 | 每个 Agent 独立的上下文窗口和记忆隔离 | ✅ 已通过 agent_id scope 实现 |
| 低 | Agent 协作可视化 | 前端展示 Agent 间任务流转和委托关系 |
| 低 | BullMQ 分布式调度 | 支持多实例部署的任务队列（当前为单进程内存调度） |

## 中期规划

| 优先级 | 功能 | 说明 |
|--------|------|------|
| 中 | Tool MCP 标准化 | 将现有工具迁移到 MCP（Model Context Protocol）标准接口，实现跨平台复用 |
| 中 | 层级化 Supervisor | Agent 数量超过 10 个时，引入多级 Supervisor（顶层管理领域 Supervisor，领域 Supervisor 管理 Worker） |
| 低 | Agent 协作可视化 | 前端实时展示 Agent 任务流转图（路由决策、执行状态、工具调用链路） |
| 探索 | A2A 协议 | 探索 Google Agent-to-Agent Protocol，实现跨系统 Agent 互操作 |

## Avatar 演进路线

详细计划见 [2026-04-22-avatar-live2d.md](./2026-04-22-avatar-live2d.md)

| Phase | 功能 | 状态 |
|-------|------|------|
| 0 | Live2D 基础框架 + 独立体验页 | 已完成 |
| 1 | 情绪驱动（SSE + 表情自动映射） | 待实现 |
| 2 | TTS 口型同步 | 待实现 |
| 3 | 模型管理与多角色切换 | 待实现 |
| 4 | 与 ChatView 集成（悬浮窗/全屏替代） | 待实现 |
| 5 | 进阶交互（视觉感知/3D 形象/硬件） | 探索 |


## 其他

- **记忆能力**：从聊天记录中提取用户信息、偏好、用户要求记住的内容
- **RAG 个人知识库**：基于检索增强生成的个人知识管理
- **日历日程管理**：查看和管理日程，日常展示日程信息
- **知识图谱 Agent**：基于本地知识库实现能力扩展，知识图谱等
- **Agent 形象**：在各系统右上角展示，可获取用户当前浏览内容和需求
- **Multi-Agent 多智能体系统**