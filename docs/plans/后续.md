# 后续计划

> 已完成的演进记录见 [changes/](../../changes/) 和 [2026-04-12-agent-system-overhaul.md](./2026-04-12-agent-system-overhaul.md)

## 待实现

| 优先级 | 功能 | 说明 |
|--------|------|------|
| 高 | HITL（Human-in-the-Loop）确认机制 | `execute_command`、`write_file` 等高风险工具执行前需用户确认 |
| 高 | 旧 `graph.builder.ts` 清理 | `llm/` 目录已删除，但 `langgraph.service.ts` 仍 import `buildGraph`，需迁移到 Supervisor 后移除 |
| 中 | Token 计算 + 消息截断 | 无滑动窗口，依赖模型自身截断，长对话会丢失上下文 |
| 中 | per-Agent 上下文管理 | 每个 Agent 独立的上下文窗口和记忆隔离 |
| 低 | Agent 协作可视化 | 前端展示 Agent 间任务流转和委托关系 |
